import { E as EthiopicDate, G as GregorianDate, a as Era, W as WeekdayIndex, C as Calendar, Y as YearProgress } from './types-Vvy_kQEv.js';
export { H as Highlight, c as HighlightCategory, b as HighlightWithDate, R as ResolvedHighlight } from './types-Vvy_kQEv.js';

/** Ethiopic epoch (1 Meskerem 1 AM == 8-08-29 (Julian)). */
declare const ETH_EPOCH = 1723856;
/** AM ↔ AA delta: AA 5501 = AM 1 */
declare const AMETE_MIHRET_DELTA = 5500;
/** Ethiopic leap year: year % 4 === 3 (Amete Mihret numbering). */
declare function isEthiopicLeapYear(year: number, era?: Era): boolean;
/** Days in Ethiopic month. */
declare function ethiopicDaysInMonth(year: number, month: number, era?: Era): number;
/** Ethiopic → JDN (AM numbering by default). */
declare function ethiopicToJdn(year: number, month: number, day: number, era?: Era): number;
/** JDN → Ethiopic (AM numbering). */
declare function jdnToEthiopic(jdn: number): {
    year: number;
    month: number;
    day: number;
};
/** Ethiopian → Gregorian. */
declare function toGregorian(ed: EthiopicDate): GregorianDate;
/** Gregorian → Ethiopian (AM). */
declare function toEthiopic(gd: GregorianDate): EthiopicDate;

declare const isGregorianLeapYear: (year: number) => boolean;
declare function gregorianDaysInMonth(year: number, month: number): number;

/** Gregorian → JDN (valid for all proleptic Gregorian dates). */
declare function gregorianToJdn(y: number, m: number, d: number): number;
/** JDN → Gregorian (proleptic). */
declare function jdnToGregorian(jdn: number): GregorianDate;
/** 0=Sunday..6=Saturday; derived from JDN modulo 7. */
declare function weekdayFromJdn(jdn: number): WeekdayIndex;

/** Get today's date in UTC as Gregorian or Ethiopic (no timezones). */
declare function today(calendar?: Calendar): EthiopicDate | GregorianDate;
/** Add days via JDN for perfect cross-month/year arithmetic. */
declare function addDays<T extends EthiopicDate | GregorianDate>(date: T, days: number, calendar: Calendar): T;
/** Add months respecting calendar-specific month lengths. */
declare function addMonths<T extends EthiopicDate | GregorianDate>(date: T, months: number, calendar: Calendar): T;
/** Add years with clamping (e.g., Feb 29 → Feb 28 when needed). */
declare function addYears<T extends EthiopicDate | GregorianDate>(date: T, years: number, calendar: Calendar): T;
/** Syntactic sugar utilities. */
declare const previousDay: <T extends EthiopicDate | GregorianDate>(d: T, cal: Calendar) => T;
declare const nextDay: <T extends EthiopicDate | GregorianDate>(d: T, cal: Calendar) => T;
declare const lastWeek: <T extends EthiopicDate | GregorianDate>(d: T, cal: Calendar) => T;
declare const nextWeek: <T extends EthiopicDate | GregorianDate>(d: T, cal: Calendar) => T;
declare const lastMonth: <T extends EthiopicDate | GregorianDate>(d: T, cal: Calendar) => T;
declare const nextMonth: <T extends EthiopicDate | GregorianDate>(d: T, cal: Calendar) => T;
declare const lastYear: <T extends EthiopicDate | GregorianDate>(d: T, cal: Calendar) => T;
declare const nextYear: <T extends EthiopicDate | GregorianDate>(d: T, cal: Calendar) => T;
declare const lastCentury: <T extends EthiopicDate | GregorianDate>(d: T, cal: Calendar) => T;
declare const nextCentury: <T extends EthiopicDate | GregorianDate>(d: T, cal: Calendar) => T;
/** Days until next 1/1 (or Meskerem 1), and % of year completed. */
declare function yearProgress(date: EthiopicDate | GregorianDate, calendar: Calendar): YearProgress;

export { AMETE_MIHRET_DELTA, Calendar, ETH_EPOCH, Era, EthiopicDate, GregorianDate, WeekdayIndex, YearProgress, addDays, addMonths, addYears, ethiopicDaysInMonth, ethiopicToJdn, gregorianDaysInMonth, gregorianToJdn, isEthiopicLeapYear, isGregorianLeapYear, jdnToEthiopic, jdnToGregorian, lastCentury, lastMonth, lastWeek, lastYear, nextCentury, nextDay, nextMonth, nextWeek, nextYear, previousDay, toEthiopic, toGregorian, today, weekdayFromJdn, yearProgress };
